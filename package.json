{
  "name": "ak-translations",
  "version": "1.0.0",
  "description": "Translation validation service with React frontend and Python backend",
  "scripts": {
    "bootstrap": "npm run bootstrap:frontend && npm run bootstrap:backend",
    "bootstrap:frontend": "cd frontend && npm install",
    "bootstrap:backend": "cd backend && poetry run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && poetry run uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
